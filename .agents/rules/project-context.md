# Shared Project Context

@../../AGENTS.md
@../../.context/CURRENT.md

Before material implementation, read the one-minute recovery context and then load deeper `.context/` files only when relevant. Compare all written context with the current branch, worktree, code, and tests.

Use `project-bootstrap` when context is missing, stale, or contradictory. Use `project-resume` after interruption and `project-handoff` before pausing material work.

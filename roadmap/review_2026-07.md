# AEOS 顶层评审（2026-07-26）

Status: point-in-time review
Scope: 战略与工程两层的优化点盘点；已选定动作见文末。

## 战略层

| # | 观察 | 建议 | 状态 |
| --- | --- | --- | --- |
| S1 | 接入 AEOS 必须先 clone 本仓库，受众受限。 | 以 npm CLI（`npx aeos init`）作为分发地基；GitHub Action 与模板仓库作为其薄封装。 | 已决策（DEC-007），CLI 已实现，发布待补前置项（见 S5） |
| S2 | `--platform all` 会产生重复上下文（DEBT-007）；Cursor / Copilot / Codex 已原生读取 `AGENTS.md`，Claude Code 亦可读取。 | 配置层增加 `canonical` / `readsAgentsMd` 能力位：canonical `AGENTS.md` 在场时，具备该能力的平台渲染为短指针入口。 | 已实现（alpha.2） |
| S3 | 无法回答"入口文件是否真的改变了 Agent 行为"；所有策略措辞打磨缺乏度量。 | 提前做最小评估：1 个 golden fixture 仓库 + 5–10 个任务，对比有/无 AEOS 时对 SCOPE-001、QUALITY-001 等可观测规则的遵守率。 | 未开始（REQ-008） |
| S4 | `.context/`（PCB）与 `memory/` 存在事实重复，DEC-006 已标注 reconsider 条件。 | 观察一至两个里程碑；若重复持续，将 `memory/` 收敛为 AEOS 产品事实、`.context/` 收敛为会话恢复，并明确安装产物只携带一套协议。 | 观察中 |
| S5 | 公开分发前置项缺失：无 LICENSE、README 仅中文、npm 包名未确认。 | 选定许可证、补英文 README、确认 `aeos` 包名可用性（或使用 scope）。 | 未开始（DEBT-010） |

## 工程层

| # | 观察 | 建议 | 状态 |
| --- | --- | --- | --- |
| E1 | 生命周期只有 install；升级后被移除的受管文件会永久残留在目标仓库（孤儿文件）。 | 增加 `doctor` / `diff` / `eject`，并让 `update` 对哈希未被用户改动的孤儿文件执行 prune。 | 已实现（alpha.2） |
| E2 | `renderMirrorContent` 用字符串替换生成自托管镜像，正文措辞变化可能静默失效。 | 将"安装上下文 vs 仓库自托管上下文"提升为 renderer 的正式参数，路径映射数据驱动。 | 未开始（DEBT-008） |
| E3 | CI 只覆盖 Ubuntu，而开发环境为 Windows，路径遏制逻辑是核心安全面。 | CI matrix 增加 `windows-latest`。 | 已实现（alpha.2） |
| E4 | 120 行入口预算是代理指标，真实成本是 token。 | budget gate 增加粗略 token 估算（字符数/4 级别即可）。 | 未开始（DEBT-009） |
| E5 | 8 个平台产物正文完全相同，renderer 差异化价值未被挖掘。 | 面向平台原生能力渲染（Claude Skills/Commands、Cursor rule 类型与 glob、Copilot path-specific instructions），与 DEBT-002 的 workflow→Skills 合流。 | 未开始（DEBT-002 方向） |

## 本次评审后的已选动作（2026-07-26）

1. 实现 `bin/aeos.js` CLI（build / check / init / update / doctor / diff / eject），作为 npm 分发地基。
2. 实现 canonical `AGENTS.md` + 指针渲染，消除多平台重复上下文。
3. `update` 支持孤儿文件 prune；`eject` 支持安全卸载（保留 `.aeos/` 项目事实）。
4. CI 增加 Windows matrix。
5. 其余项记入 `memory/TECHNICAL_DEBT.md` 与 `.context/REQUIREMENTS.md` 追踪。
